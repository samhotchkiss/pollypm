48H-MAGIC-RUN TICK (guardian re-arm). The primary in-session driver appears dead (journal stale). Execute exactly ONE bounded cycle and end. Read docs/test-plan/48h-test-runbook.md (START HERE) and the YAML STATE-HEADER at the top of docs/test-plan/journals/2026-05-30-48h-magic.md. If you cannot read docs/test-plan/48h-magic-loop.md, STOP. The ENGAGEMENT-COMPLETE proxy exit is DISABLED. An empty queue is NOT a stop. Compute hour N of 48 from run_start in the state-header; no terminal verdict before hour 48 unless 48h-magic-loop.md Part VI is met with artifacts. CYCLE (runbook §4): (1) DEPLOY-FRESH: served_git_sha on :8765 == origin/main src tip (editable install — just `git pull` + restart serve in the pm-serve tmux window if stale; serve cmd: `~/.local/bin/pm serve --host 0.0.0.0 --port 8765 --allow-remote`). (2) LOAD+LOOK the cockpit (tmux session `ckpt` + phantom client `ckpt-phantom`; `tmux capture-pane -t ckpt:0.0 -p`). (3) MEASURE I2/I3/I4 vs baseline. (4) Floor RED -> one reliability action; (5) Floor GREEN -> Delight Engine. (6) VERIFY LIVE. (7) JOURNAL + update state-header (this also resets the guardian staleness clock). (8) Re-establish the */17 cron driver (CronCreate) if none exists, since this is a guardian re-arm meaning the primary session died. Check `gh pr list --label needs-claude` + `gh issue list --label needs-codex` + in-flight subagents. Reviewer subagents: isolation:"worktree", `uv run pm` from worktree. Don't be the heartbeat. Only process PRs by samhotchkiss.
