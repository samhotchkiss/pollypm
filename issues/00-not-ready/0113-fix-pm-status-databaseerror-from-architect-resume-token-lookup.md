# Fix pm status DatabaseError from architect resume token lookup

During watchdog escalation for samblog/24, task-level actions succeeded: samblog/3 moved to in_progress with an active implement execution and duplicate draft samblog/24 was cancelled. A separate global status probe failed with sqlite3.DatabaseError: database disk image is malformed from get_architect_resume_token while reading /Users/sam/.pollypm/state.db. Follow-up triage on 2026-05-18: sqlite3 PRAGMA quick_check returned ok, SELECT count(*) FROM architect_resume_tokens returned 0, PRAGMA integrity_check(architect_resume_tokens) returned ok, but pm status still reproducibly crashes through pollypm/plugins_builtin/default_launch_planner/planner.py -> ctx.store.get_architect_resume_token. Investigate why supervisor-wide status can report a malformed database despite direct integrity checks passing, and implement the narrow fix or repair path.

## Acceptance Criteria

pm status no longer raises sqlite3.DatabaseError from get_architect_resume_token on this workspace. The fix distinguishes true SQLite corruption from a recoverable/empty architect_resume_tokens lookup and does not mask unrelated database errors silently. Regression coverage exercises the status/launch-planner path when architect_resume_tokens is empty or otherwise readable. Any operational repair step is documented in the task output, including whether /Users/sam/.pollypm/state.db needed backup/checkpoint/reindex/rebuild action.
