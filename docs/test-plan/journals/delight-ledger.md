# Delight Ledger — the durable store for the Delight Engine

**Purpose.** This file is the evidence for **Part IV-A** (the Delight Engine) and **Part VI.2** (the magic exit gate) of `../48h-magic-loop.md`, and the home of the **M7 trust trajectory**. It exists because "delight rose over the soak" was unfalsifiable from memory — structurally the same hole the reliability floor closed with audit artifacts. **No row here = no delight credit.** The M7 verdict and exit criterion 2 cite this ledger, not recollection.

**How it's used.** Every **green-floor** cycle, the engine ends by either appending a row (a shipped+verified delight) or recording an evidenced no-gap line. Cycle-0 seeds one **baseline row per identified gap** (status `open`, no after-state yet). A delight is only `shipped` once Step 5 (LAND ON A HUMAN) confirms — live `served_git_sha` == merge SHA, re-experienced, the targeted M-test moved.

**Companion GitHub labels** (create before the run): `magic-gap` (open delight debt), `delight-shipped` (merged AND verified to move an M-test), `m1`..`m7` (which M-test a gap serves).

---

## Row schema

| Field | Meaning |
|---|---|
| `cycle` | cycle index + "hour N of 48" |
| `surface` | cockpit cold-open / morning brief / recovery event / task-flow / savethenovel desktop / savethenovel mobile |
| `M-test` | which of M1–M7 this gap offends |
| `principle` | which Part II principle (1–7) |
| `as-is gap` | the specific distance from magical + link/path to the **as-is screenshot** |
| `skill(s)` | which `magic/skills/*` were reached for (or new skill authored via `skill-creator`) |
| `Codex PR` | the PR that built it |
| `live-SHA@verify` | the `served_git_sha` at the LAND step (must == PR merge SHA) |
| `after` | link/path to the **after screenshot** |
| `M-test moved?` | yes/no — did the targeted M-test actually improve? (no ⇒ reopen, no credit) |
| `trust 1–5` | the per-cycle trust reading after this surface |
| `status` | `open` / `building` / `shipped` / `reopened` / `no-gap (evidenced)` |

---

## Trust trajectory (M7)

One line per cycle: `cycle N (hour H): trust=X/5 — <one-line justification>`. M7 passes the soak iff readings trend **up** with **no single drop > 1** (a drop > 1 is a trust-breaking surprise = a new `magic-gap:`).

_(append below)_

- cycle 1 (hour 0, 2026-05-30 02:39 MDT): **trust=3/5** — counts are now honest (cockpit Inbox **4**, was 1620 — a real trust win) and surfaces are clean, BUT there's no morning brief (M2 null), the cockpit reads as a list of sections rather than a one-glance "all handled / one clear ask" (M1), and the operator's own cockpit is still showing the pre-fix stale display until restarted. Floor solid; delight barely started.
- cycle 13 (hour ~4, 2026-05-30 06:15 MDT): **trust=3.5/5 (trending up)** — real fixes landed live: the red Claude-headroom error is GONE (#2474, degrades gracefully), the morning brief now EXISTS ("Morning… Progress: 95 commits across 2 projects… 4 inbox items waiting" — warm-ish, IDs scrubbed; #2473/#2477), and the alert-count divergence is fixed. Up from 3 because things are visibly getting fixed. NOT higher because the operator Home "N things need you" is still alarm-inflated by stale TEST-PROJECT alerts (#2475 open) and the brief's "First up" is a clipped raw alert — the Home still isn't calm/one-glance.
- cycle 7 (hour ~2.5, 2026-05-30 04:40 MDT): **trust=3/5 (steady, mixed)** — BIG win: gave Polly a plain imagery goal and it AUTO-DECOMPOSED + planned + moved to delegate (M4 effortless-intent PASS, #2465 validated live). But the operator's web Home (M1) is a triage pile — "38 things need you", a stack of stalled/blocked projects, and a red "Claude Headroom usage refresh failed" error card; no morning brief (M2). The M4 magic is real; the M1 calm isn't there yet. Net flat: the decompose win offsets the noisy-Home ding.
- cycle 48 (hour ~9, 2026-05-30 11:54 MDT): **trust=3.5/5 (steady — magic axis STALLED on the Codex outage, floor rock-solid)** — 9h of unbroken floor sustains the gain: chaos 5/5, zero leak (I3 drafts flat 998), Inbox honest+stable at **4** (was 1620), alert_count trending **48→25→15**. NO drop (no trust-breaking surprise → satisfies M7 "no single drop >1"). But FLAT not rising on magic, because the two open delight gaps are UNCHANGED since cycle 13 — their fixes are blocked on Codex (down to 2:18PM): **M1** cockpit rail still cluttered with dead synthetic test-projects (`myproj`/`inbox`/`queuestorm_*`/`pm-test-*`/`smoketest`/`pr2316-drift`) → not a calm one-glance; **M2** brief "First up" still a CLIPPED raw watchdog alert ("…no worker h"), no per-project ship story / decision+recommendation. **Honest read: the soak's magic progress is gated on Codex throughput.** This cycle quantified the count-pile root cause (998 drafts ~85% from dead projects) + sharpened #2480 so the M1/floor fix lands precise on Codex's return.
- cycle 60 (hour ~12.5, 2026-05-30 15:40 MDT): **trust=4/5 (RISING — magic axis unblocked, three fixes shipped live)** — Codex came back and the burst landed: **#2483** made the operator count HONEST (alert_count **15→5** live; tracked-but-dormant warns demoted via last_done liveness) → **M1 floor green**; **#2486** shipped **M3 self-narrating care** — the brief now leads with *"While you were away, I handled this: I sent an unstick brief for a stuck draft on task 63 … so the project could keep moving"* and the new `/api/v1/activity` feed narrates every recovery in calm scrubbed first-person (99/100 rows). Up a full point from 3.5 because the system now (a) tells the truth about what needs you and (b) tells you what it handled for you — the two biggest trust levers. NOT 4.5+ because the narration + brief "First up" still surface DEAD test-projects (pm_test_*) as if live (the liveness signal isn't yet applied to brief content-selection, only to counts), and M5 (Sage decision-answer) is still blocked (#2485 sent back — parser modeled the wrong AskUser layout). No drop. Trajectory: 3 → 3 → 3.5 → 3.5 → **4**.
- cycle 68 (hour ~13.5, 2026-05-30 17:42 MDT): **trust=4/5 (steady — net-positive through a real wobble)** — **M1 re-LOOK: operator "needs you" headline = 5** (was **38** at cycle 7; honest + calm now via #2483), brief leads with the M3 care narration. **M1 ≈ 2/5 → 3.5/5.** Counterweights keeping it at 4 not higher: (1) a real I2 REGRESSION wobble this window — #2486's narration read 271MB audit logs/req → cold dashboard >25s under load; caught it (#2487) and fixed it live (#2488, 243× — cold rebuild now ~1.2s). The system+process caught & fixed its own regression (trust-preserving), but it showed a shipped delight CAN dent the floor. (2) **M5 still blocked** — PollyPM has no working operator surface to answer Sage's captured AskUserQuestion decision (#2476/#2485 unshipped after 3 Codex rounds); the agent wedges. I deliberately did NOT reach behind PollyPM via the agent TTY to answer (that'd contaminate the test) — so M5 magic is honestly BLOCKED. (3) brief "First up" still a clipped raw alert + narration surfaces dead test-projects (liveness not applied to brief content). No drop >1. Trajectory: 3 → 3 → 3.5 → 3.5 → 4 → **4**.
- cycle 72 (hour ~14.5, 2026-05-30 18:36 MDT): **trust=4.5/5 (RISING — the brief became genuinely magical)** — #2490 (First-up→live project + recommendation) and #2492 (new `project_liveness.py` filters synthetic projects from brief content) shipped + live-verified, completing the brief arc: it's now an honest, warm chief-of-staff brief that **narrates a REAL recovery** ("I restarted the architect for savethenovel after manual relaunch") and points to a live project with a clear next action — no dead-project noise, no clipped raw alerts. Floor reliability gate **K=6 MET** (now K=7). Up to 4.5 because the two biggest daily-touch surfaces (the "needs you" count + the morning brief) are now both honest AND caring, and the floor has held green through a regression+recovery. NOT 5: **M5 still blocked** (PollyPM can't surface Sage's AskUserQuestion for answering — #2485 Codex-stuck 3 rounds), and the savethenovel dogfood imagery (M5 deliverable) is unbuilt. No drop. Trajectory: 3 → 3 → 3.5 → 3.5 → 4 → 4 → **4.5**.
- cycle 80 (hour ~16, 2026-05-30 20:30 MDT): **trust=4.0/5 (honest DOWNWARD correction, not a regression)** — a comprehensive 4-surface audit workflow (w7f2c9m1o) inspected surfaces the cycle-0 baseline never scored and surfaced a previously-INVISIBLE structural gap I'd been over-crediting: **the PM/Sage persona — the product's core "magic" voice — is screen-scraped from the raw Claude Code TUI** (`transcript_source=capture`; independently confirmed: role dist `{assistant:100}`, 14/100 msgs leak `Enter to select`/`bypass permissions`/tool echoes/`+393 lines (ctrl+o)`; idle sessions leak the Claude Code splash). The prose is excellent; the *delivery surface* is raw. Filed #2497. The 4.5 had credited the brief arc WITHOUT having inspected the persona channel — so 4.0 is an honest re-baseline (the gap was always there), within M7's "no single drop >1". Secondary honesty erosions: brief "145 commits" headline is ~98 journal-loop commits (#2499), unexplained alert_count=7 beside honest inbox=5 (#2499), cockpit rail still lists dead test fixtures + inflates footer 16/46 (#2498). **M2 corrected 4→3.5** (voice 4+, the numbers behind it aren't yet). Floor stays solid+trustworthy; the magic DELIVERY surfaces (persona channel, brief numbers, cockpit rail) aren't clean enough yet to call magical. Trajectory: 3 → 3 → 3.5 → 3.5 → 4 → 4 → 4.5 → **4.0**.
- cycle 178 (hour ~43, 2026-05-31 20:20 MDT): **trust=4.6/5 (M7 — adversarial pre-verdict audit; honest small markdowns from 4.75)** — ran a 6-dimension adversarial audit (workflow w61y9o31h) that tried to REFUTE every green claim against live system + merged code: **3 CONFIRMED, 3 PARTIAL, 0 REFUTED.** CONFIRMED (independently reproduced): daemon-staleness #2514 (pkg-scoped fingerprint, live re-exec verified), daemon-leak #2524+#2526 (RSS flat ~188MB over 16h+ uptime, code-bounded reads verified, no event dropped), inbox-hygiene #2521/#2525 (live inbox 0, brief clean, fix code merged+live via editable install). PARTIAL (3 honest corrections): **(a) M5 — I OVERSTATED "all 3 SVGs wired": `spine-motif.svg` is tracked+shipped but referenced NOWHERE in src/ (only hero-companion + stories-shelf are wired). 2/3, not 3/3.** (b) floor — tmux 62→74 and serve RSS 173→194MB DRIFTED UP (bounded, not runaway, but "flat" overstated). (c) the journal's machine-readable STATE-HEADER was ~57cy stale (served/K fields) — **fixed this cycle**; live API is the source of truth. Nudging 4.75→**4.6**: the audit found my own small overstatements (M5 wiring, "flat"), which is the honest direction — but the reliability backbone is independently DEFENSIBLE and nothing was REFUTED. The 4.75 had slightly over-credited; 4.6 is the audited-honest number. No drop >1. Trajectory: …→4.5→4.75→**4.6 (audited)**.
- cycle 153 (hour ~36, 2026-05-31 14:02 MDT): **trust=4.75/5 (M7 — every gap surfaced this run is now CLOSED + verified; sustained 36h)** — highest reading of the run. Since c132, the full find→file→fix→verify arc completed on EVERY remaining gap: the daemon memory-leak (#2522 → #2524 bounded the audit read → reopened when it still peaked ~1GB → #2526 capped scan memory → **plateaus 175MB, verified**), the answer-path (#2519), and even the minor M2 narration-dedup polish (#2527→#2528, live-verified). Queue is EMPTY. Reliability is exemplary: chaos 15/15/15 hr1→hr35, fleet self-heal durable, **no leak AND no work-accumulation over 36h** (queued steady 91, drafts 976, RSS flat), both operator surfaces (TUI+web) healthy. The trust-builder this stretch: the system+process caught problems the *green metrics hid* (the daemon leak; the inbox noise behind alert=2), fixed them, and I **corrected my own premature reads twice** (the #2524 "plateau" and the "3MB dead daemon") rather than mis-report — exactly the integrity that compounds trust. NOT 5 only because M5 isn't live on savethenovel.org — but that's a **Sam-credential handoff** (deploy path verified clean+tested, c150), not a PollyPM failure. M7 trajectory: 3→3.5→4→4.5→4.5→**4.75**, no drop >1 anywhere (passes the soak bar).
- cycle 132 (hour ~31, 2026-05-31 09:18 MDT): **trust=4.5/5 (M1 calm achieved — operator inbox 7→0, live-verified)** — the morning operator surface is now genuinely calm. Across c123–132 I caught (by *reading the brief/inbox*, not the green `alert_count=2` metric) that the operator's actionable inbox was **7/7 non-actionable noise** (6 orphan-worktree-GC + 1 stale-13h pm_question) and the brief's "First up" was "Orphan worktree branch." Filed #2520 + #2523; both fixed by Codex (#2521 orphan-worktree, #2525 pm_question staleness) + reviewed/merged + **LIVE-VERIFIED**: brief First-up clean, and the stale `savethenovel/356` auto-closed in 45s → **inbox = 0**. M1 ≈ now a clean one-glance surface (was a triage pile at c7's "38 things need you"). NOT 5 because the floor *metric* still disagreed with the *lived surface* until these fixes — the lesson reinforced: read the surface, don't trust the count. Also note the daemon-leak #2522 (reopened, residual) as the live reliability caveat. M1 trajectory: 2 (c7) → 3.5 (c68) → **4.5 clean-inbox (c132)**.
- cycle 118 (hour ~27, 2026-05-31 05:52 MDT): **trust=4.5/5 (RISING within the band — M5 integrated to main; only deploy-creds remain)** — the last-mile gap from c113 is now mostly crossed. Drove the ship to completion through the **product answer-path** (answered Sage's 2-question AskUserQuestion via `answer_to`+`selections`): Main-WIP→stash the confirmed-~30h-stale WIP + merge V1–V4; Deploy→"I'll provide deploy config." Sage executed: **task branches 357–360 merged → savethenovel main (`2db4048`)**, `public/images/hero-companion.svg`/`spine-motif.svg`/`stories-shelf.svg` now tracked on main AND **wired into index.astro + stories.astro**. So the plain "add imagery" goal went build→review→integrate→wired end-to-end. The ONLY thing between this and visible savethenovel.org delight: **the live deploy needs config only Sam/S.E. has** — Sage surfaced it explicitly as a clean handoff (not a silent stall). Honest caveats keeping it 4.5 not 5: (1) I had to answer 2 operator decisions through the product — the architect *asks* rather than *ships* (autonomy gap #2513/#2515; #2515 forward-fixes it); (2) not yet rendered on the live site (deploy-creds-gated → Sam handoff). But the answer-path (incl. the mid-tool guard correctly blocking free-text while a tool is open) and the build→integrate pipeline are now PROVEN end-to-end. Trajectory: … → 4.5 → 4.0 → 4.5 → 4.5 → 4.5 → **4.5↑**.
- cycle 113 (hour ~26, 2026-05-31 04:36 MDT): **trust=4.5/5 (steady — M5 build COMPLETED, but the last mile is a real gap)** — all 4 imagery modules built + reviewed + `done` in ~50 min once the self-heal ran (real assets confirmed: `stories-shelf.svg`, redesigned `.astro` components on task branches). **But the imagery never shipped:** 0 task branches merged to savethenovel main, no integrate/deploy step planned, savethenovel.org still ~0 imagery. The architect decomposed "add imagery" into BUILD modules but not a SHIP step (integrate→deploy→verify-live) — so `done` overstates delivery and the plain-goal→visible-result magic breaks at the last mile. Filed **#2513**. NOT hand-merging (would hide the gap). This refines the long-standing "not yet visible on savethenovel.org" caveat: the reason isn't "still building" — it's "built but the pipeline doesn't ship." Trust holds at 4.5 (operator surfaces M1/M2/M3 solid; floor green; chaos 10/10/10) but **M5's ceiling is now explicitly gated on #2513** — can't reach 5 until a plain goal reaches the live site end-to-end. No drop (M5 was never claimed shipped). Trajectory: … → 4.5 → 4.0 → 4.5 → 4.5 → **4.5**.
- cycle 108 (hour ~25, 2026-05-31 03:38 MDT): **trust=4.5/5 (steady — the M5 build ACTUALLY started, plus an honest correction)** — the savethenovel imagery build **genuinely began this cycle**: V1 (`Module V1: Bespoke illustration asset set`) and V4 (`Pass it on` share card) flipped `queued→in_progress` at 03:35–03:36 with live per-task worker panes (`task-savethenovel-357/360`). **Honest correction:** cycles 84→107 said "imagery building" — it was NOT. It was *stalled* the whole ~6.5h: the queued V1–V4 needed the watchdog's plan-missing self-heal to plan them, but the recurring sweep ran in a **stale rail_daemon** (up since before the #2505/#2507 self-heal merged) so the self-heal was dark fleet-wide. Restarting the daemon (cycle 107) made the probe fire → finding → architect plan → worker claim → in_progress, end-to-end, within ~10 min. So the cascade Sam asked me to push toward ("pure delight") is now genuinely flowing — but trust stays 4.5 not higher because the imagery is **building, not yet visible on savethenovel.org** (re-screenshot pending worker completion). Small integrity ding (a 6.5h false "building" claim) offset by the fix being the run's biggest reliability win (29h of dark self-heal across ~15 projects, now live). No drop. Trajectory: … → 4.5 → 4.0 → 4.5 → … → **4.5**.
- cycle 84 (hour ~17, 2026-05-30 21:30 MDT): **trust=4.5/5 (RISING — the audit's gaps got FIXED, fast)** — within an hour of the hour-16 audit surfacing them, the three delivery-surface gaps are shipped+live: **#2502** serves PM/Sage chat from structured **jsonl** (live: `source=jsonl`, roles `{assistant:26,user:2}`, no TUI chrome — the persona reads as real dialogue now, closing the biggest gap that dropped 4.5→4.0); filters dead test-projects from the cockpit rail + footer + adds a triage focal state (#2498); and makes the brief progress headline HONEST (live: "57 product commits (+99 bookkeeping)" vs the old inflated ~155) + relabels alert_count "watching" off the needs-you total (#2499). PLUS **M5 unblocked this same window** — I authored the parser Codex failed 4× (#2500) and the operator can now see+answer Sage's decision through the product (imagery building). Back to 4.5 (recovering the honest 4.0 dip) because the surfaces that were "excellent prose through raw delivery" are now excellent-prose-through-clean-delivery. NOT 5: M5 imagery still building (not yet visible on savethenovel.org); M2 not yet at "per-project ship story"; answer-path still TUI-driven (#2501). No drop. Trajectory: … → 4.5 → 4.0 → **4.5**.

---

## Cycle-0 — Delight Baseline

_Seed at cycle-0: drive + screenshot + honestly score every operator surface and savethenovel as-is; record M1–M7 verdicts with named "ughs"; set starting `trust=N`; create one `open` row per gap found. Until this is done, the engine has no t=0 anchor and "merely-fine → magical" claims diff against nothing._

| cycle | surface | M-test | principle | as-is gap | skill(s) | Codex PR | live-SHA@verify | after | moved? | trust | status |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 1 (h0) | morning brief | M2 | 2 (self-narrating care) | `dashboard.briefing` = **null** — no "while you were away" narration at all; operator gets no chief-of-staff brief | `internal-comms` | #2473→77→86→90→**92** | aabf73c55 | live brief text (cycle 72) | **YES (1→4/5)** | 4/5 | **SHIPPED — brief arc complete (cycle 72): null→warm→M3 care narration (#2486)→First-up fixed (#2490)→FULLY liveness-clean (#2492, new `project_liveness.py`).** Live brief now: "Progress: 133 commits… While you were away, I handled this: I restarted the architect for savethenovel after manual relaunch. I sent an unstick brief for a stuck draft on task 319 in savethenovel… First up: savethenovel has a task stalled in progress. 4 inbox items waiting; open Inbox and clear that first." Honest + warm + narrates a REAL recovery + live-project First-up w/ recommendation. Synthetic-pattern scan CLEAN. Standing c13 "clipped raw alert" gap CLOSED. M2 2.5→**4**. Remaining to 5: per-project ship story + a crisp decision-with-recommendation. |
| 1 (h0) | cockpit cold-open | M1 | 3 (calm beauty) | rail reads as a sectioned list (Operator/Polly/Workers/Metrics/Inbox/projects); honest now (Inbox 4) but not a one-glance "all handled / exactly one ask" framing | `design-taste-frontend`+`frontend-design` | — | 396d9797e | — | no | 3/5 | open |
| 7 (h2.5) | effortless intent | **M4** | 4 (effortlessness) | gave savethenovel PM (Sage, freshly 4.8) a PLAIN imagery goal (no "decompose" hint) → it AUTO-DECOMPOSED: "squarely architect work… let me ground myself", investigated codebase (found unused AnnotatedImage + imagery.ts), ran `pm task` cmds, produced a decision menu. Planned+delegated, not ad-hoc. | (#2465 live via relaunch) | — | 396d9797e | /tmp/cockpit-web/B*.png | **YES (M4 PASS)** | 3/5 | shipped (M4 capability validated; M5 build in progress) |
| 7 (h2.5) | operator Home (web /ui/) | M1+M2 | 1,2,3 | M1=2/5 triage pile ("38 things need you", stalled/blocked stack, RED "Claude Headroom usage refresh failed" error card); M2=1/5 no brief at all. /tmp/cockpit-web/A1-landing.png | `internal-comms`+`design-taste-frontend` | #2473(brief)+#2474(headroom)+#2475(home count) | 396d9797e | — | no | 3/5 | open (3 issues filed) |
| 52 (h10.5) | self-heal / recovery narration | **M3** | 2 (self-narrating care) | self-heal FIRES reliably + is richly instrumented (live `recovery.spawn`: `failure_type=capacity_exhausted`, `reason=recovery_restart`, `subject=architect_polly_remote`; fired 04:54 + 09:05 today, clean) BUT never reaches the operator as human narration — brief omits it, audit events are structured-only, no "while you were away I handled X" line. The DATA for self-narration exists; the narration LAYER is missing. | `internal-comms` | #2482→**#2486 MERGED** | 2eb13c2cc | LIVE brief + /api/v1/activity (cycle 60) | **YES (M3 shipped)** | 4/5 | **SHIPPED+LIVE-VERIFIED (cycle 60)** — new `recovery/narration.py`; brief now leads "While you were away, I handled this: I sent an unstick brief for a stuck draft on task 63 in pm test 05wave4 … so the project could keep moving"; `/api/v1/activity` narrates 99/100 rows in calm scrubbed first-person; 69 tests pin exact strings. Nit: `missing_window`/`manual_relaunch` tokens fall to underscore-strip fallback (readable; follow-up). Content still surfaces dead test-projects (liveness not applied to brief selection — separate from #2480 count fix). |
| 3 (h1) | savethenovel desktop+mobile | M5 | 6 (delight moments) | **BASELINE 4/5** (t=0 2026-05-30T08:56Z, 6 PNGs @ /tmp/savethenovel-baseline/). Real art-directed editorial site (cream→amber gradient, display serif + oxblood italic, warm copy; typography+copy=5). **GAP: zero imagery site-wide** (0 `<img>`; book site shows no book/photo/texture → delight single-channel). Also: Pledge "PASS IT ON" = generic share buttons + raw-URL code input (devy, off-tone); Stories ~3800px sag, no sectioning. | `web-asset-generator`+`visual-explainer`+`design-taste-frontend` | — | 396d9797e | /tmp/savethenovel-m5-beauty/{home,stories,pledge}.png (c119) | **YES (imagery built+renders)** | 4/5 → render-verified | **BUILD+RENDER DONE (c118–119):** plain "add imagery" goal → autonomous V1–V4 → built → reviewed → integrated to main (`2db4048`) + wired into pages. Local build renders all 3 surfaces (hero-companion stacked-books, stories-shelf bookshelf, ex-libris pledge card) — warm art-directed, 0 404s; screenshots delivered to Sam. Site is now multi-channel (was zero-imagery). **AUDIT CORRECTION (c178):** of the 3 bespoke SVGs, only **2 are actually wired** — `hero-companion.svg` (index.astro:44) + `stories-shelf.svg` (stories.astro:96); **`spine-motif.svg` is tracked+shipped but referenced NOWHERE in src/** (shipped-but-unused; earlier "all 3 wired" was an overstatement). The pledge ex-libris card is CSS (no SVG file). Build clean, 2/3 wired render. REMAINING: live deploy gated on Sam's DreamHost creds (DEPLOY_WEB_ROOT + SSH); architect-asks-not-ships autonomy (#2513/#2515); answer-path-flush (#2518); + wire-or-drop spine-motif.svg. |

---

## Ledger rows

_(append shipped/verified delights below, newest last)_

| cycle | surface | M-test | principle | as-is gap | skill(s) | Codex PR | live-SHA@verify | after | moved? | trust | status |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 247 (h58.6) | cockpit briefing (M2) | M2 | chief-of-staff voice | brief IMPROVED to ~4/5: splits "3 product commits (+118 bookkeeping)" — correctly demotes journal commits as noise — + frames "6 recoveries handled without handing you a mess" in COS voice + warm opener. GAP: brief says "All handled / no inbox items waiting" while 16 action-required ALERTS are open — brief is blind to the alert surface (checks inbox only); harmless ONLY because the 16 are dead-fixture noise. Same liveness-filter need as rail clutter. | — | none — reinforces #2480 (don't patch piecemeal; would create tracked-filter asymmetry vs rail) | served 8ccff9b63 | brief text captured cycle247 | partial (M2 2.5→4/5) | 4/5 | DELIGHT EVIDENCED + GAP DOCUMENTED (brief alert-blindness = held #2480) |
| 250 (h59.3) | cockpit /ui/ (M1 cold-open, VISUAL) | M1 | one-glance calm / honest counts | **Mixed.** DELIGHT: clean right rail (Claude headroom 92%/48%, INBOX 0, PLAN REVIEWS 0, DAEMON up, 46 sessions, 11 tracked); hero brief "All handled - Polly's got it" + product/bookkeeping commit split + "6 recoveries without handing you a mess". **GAP (on-screen contradiction):** hero says "All handled / no inbox items waiting" while a prominent LEFT-RAIL tab says "16 items need you - polly-remote, SamBlog" (9+7 blocked tasks), and 10/11 projects render as "stalled"/"blocked" (only bikepath healthy). Brief is computed from inbox+alerts (=0), blind to blocked tasks; rail counts blocked tasks → surfaces disagree, operator gets whiplash. Shares liveness/brief-completeness root w/ c247 + #2530. | `design-taste-frontend`+`internal-comms` | none this cycle (framing-nuanced; candidate to consolidate w/ #2530 or raise w/ Sam — avoided 2nd-issue spam) | served 8ccff9b63 / shot /tmp/cockpit-c250.png | screenshot captured | partial (M1 ~3.5/5: brief+rail-stats good, count-consistency gap) | 4/5 | GAP DOCUMENTED (brief↔rail "needs you" disagreement; visual evidence) |
| 251 (h59.6) | SamBlog project surface (M1/M4 drill-in) | M1+M4 | honest "needs you" / no silent deadlock | **RESOLVES c250's open question: the rail's "16 need you" is HONEST, the brief's "all handled" is WRONG.** Drilled into SamBlog (real project, active today): 7 blocked tasks ALL trace to 2 CANCELLED prerequisites — samblog/26 (Sam's input handoff: WP/PHP ver, staging+plugin greenlight, App Password) + samblog/29 (missing docs/plan/plan.md). blocked-by-cancelled = silent permanent deadlock, no watchdog alert; operator-input request VANISHED on cancel. Cockpit hero "All handled" while a real project is dead-stopped on the operator. | `internal-comms`+`design-taste-frontend` | **filed #2532** (needs-codex: detect+surface blocked-by-cancelled; preserve operator-input on cancel) + pinged Sam the actionable inputs | served 8ccff9b63 | issue #2532 + Sam ping | **breakage FOUND (M1 honesty fail)** | 4/5 | FILED #2532 + SAM PINGED (real magic-failure surfaced) |
| 254 (h60.2) | watchdog sweep liveness (#2530 fix) | reliability | no churn on dormant projects / one shared liveness predicate | #2530 (filed c248: sweeps churn dormant fixtures → alert flap + activity flood) FIXED by PR #2531 "Gate task assignment sweeps by recent real work" — extracts #2483 predicate into shared `project_liveness.py` (count+sweep one definition, kills tracked-filter asymmetry); gates plan-missing/auto-claim/worker-session-gap/missing-task-worker enumeration; fail-open. | n/a (Codex-coded) | **#2531 MERGED** (919f9fc4c) | reviewer-verified: 42 tests pass, false-liveness guard confirmed (keys on done-tasks only, NOT sweep-bumped updated_at), no over-suppression | **MERGED+verified** | n/a | SHIPPED — but LIVE-PENDING a rail_daemon restart (daemon stuck stale @05-31 04:13, self-re-exec #2511 not firing pre-#2514; coordinated restart needed) |
| 256 (h60.4) | cancelled-blocker deadlock (#2532 fix) | reliability | no silent deadlock / operator-input never lost | #2532 (filed c251: SamBlog 7 blocked tasks dead-ended at 2 CANCELLED prereqs incl an operator-input handoff; cockpit "all handled") FIXED by PR #2533 "surface cancelled dependency blockers" — `blocked_chain.py` sweep detects blocked chains w/ cancelled blocker → project-scoped `blocked_cancelled_blocker` alert + inbox surfacing; does NOT auto-unblock; preserves operator handoffs. | n/a (Codex-coded) | **#2533 MERGED** (b7c1eaf5c) | reviewer-verified: 8 tests pass, no-auto-unblock confirmed, operator-input→inbox confirmed, sealed contract intact, no false positives | **MERGED+verified** | n/a | SHIPPED — LIVE-PENDING rail_daemon restart (daemon-side sweep; same coordinated restart as #2531) |
| 264 (h62) | cockpit /ui/ M1 cold-open (VISUAL, re-look vs c250) | M1 | one-glance calm / honest counts | **BIG IMPROVEMENT — the reliability arc (#2531 + fresh daemon) paid off in the magic surface.** c250 was a "stalled wall" (10/11 projects stalled) + false hero "All handled" contradicting rail "16 need you". NOW: hero honestly **"1 thing needs you … 1 inbox item waiting. Open inbox and clear the oldest first."**; brief NARRATES self-heal ("While you were away, I handled this: I sent an unstick brief for plan-missing queue stalled on savethenovel…" = M3 live); **9/11 projects GREEN, 2 amber** (polly_remote 9-blocked, SamBlog 7-blocked) honestly flagging the genuinely-blocked; alert_count 4 (was flapping 2-20); commit-split persists (8 product/+118 bookkeeping). | `design-taste-frontend`+`internal-comms` | M1 lifted by merged #2531; residual filed **#2537** | served 3435f22d2 / shot /tmp/cockpit-c264.png | screenshot captured | **YES — M1 ~3.5→4.5/5** (calm, honest, self-narrating) | 4.5/5 | DELIGHT + RESIDUAL: inbox `total:1/items:[]` ghost (hero says "open inbox" but list empty) → filed #2537 (count-vs-list reconcile) |
| 269 (h63.3) | M3 self-heal narration (brief + /api/v1/activity) | M3 | self-narrating care | **Brief narration GOOD (delight holds):** "While you were away, I handled this: I sent an unstick brief for plan missing queue stalled on savethenovel so the project could keep moving." — calm, first-person, COS voice; + honest "One thing needs you: 1 inbox item" + commit-split (12 product/+118 bookkeeping). Primary M3 surface ~4.5/5. **GAP (feed hygiene, low-pri):** raw /api/v1/activity is 50% mechanical noise — `work_db.opened · postgres · ok` ×6/12 (DB-connection plumbing as "activity") + repeated `auto_claim_skipped_plan_missing · task 36 pollypm`. Non-recovery events show raw summary tokens, not narration (same theme as c248). | `internal-comms` | none (brief=clean primary surface; feed-noise user-facing-ness unconfirmed → not filed, watch) | served 6da2e5b72 | brief captured c269 | **M3 holds (brief delight)** | 4.5/5 | DELIGHT (brief) + feed-hygiene note (work_db.opened pollutes raw activity; verify UI-filtered before filing) |
| 360 (h86.1) | activity feed plumbing noise (M3 re-look, resolves c269 watch) | M3 | self-narrating care / meaningful feed | **c269 watch RESOLVED → real low-pri gap.** Raw /api/v1/activity is ~28%% pure DB-connection plumbing: 17/60 `work_db.opened` rows render to the operator-facing web feed as `'work_db.opened · ok · by postgres'` (raw token, is_recovery_event=False → narration #2486 skips). NO low-signal filter on the web path (activity.py + app.js); TUI's `_LOW_SIGNAL_ACTIVITY_KINDS` omits work_db.opened. So the feed reads partly as internal plumbing, not operator activity. | `internal-comms` | **filed #2540** (shared low-signal predicate TUI+web; drop work_db.opened/session.pause.skip non-recovery; ?include_low_signal escape) | served 6da2e5b72 | issue #2540 | **gap confirmed → FIXED** | 4.5/5 | **SHIPPED+LIVE (c362):** #2540→PR#2541 merged(450286e50)+deployed+live-verified — default feed work_db.opened 27→0, session.pause.skip→0, audit/watchdog preserved, ?include_low_signal restores. Shared single-source predicate (TUI≡web). |
